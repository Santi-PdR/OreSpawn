plugins {
    id 'eclipse'
    id 'idea'
    id 'maven-publish'
    id 'net.minecraftforge.gradle' version '[6.0.24,6.2)'
}

version = '3.3.0'
group = 'com.santipdr.copyl'

base {
    archivesName = 'orespawn-reforged'
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(17)
}

minecraft {
    mappings channel: 'official', version: '1.20.1'

    runs {
        configureEach {
            workingDirectory project.file('run')
            property 'forge.logging.markers', 'REGISTRIES'
            property 'forge.logging.console.level', 'info'

            mods {
                copyl {
                    source sourceSets.main
                }
            }
        }

        client {}

        server {}

        data {
            workingDirectory project.file('run-data')
            args '--mod', 'copyl', '--all', '--output', file('src/generated/resources/'), '--existing', file('src/main/resources/')
        }
    }
}

sourceSets.main.resources { srcDir 'src/generated/resources' }

repositories {
    mavenCentral()
    maven { name = 'GeckoLib'; url = 'https://dl.cloudsmith.io/public/geckolib3/geckolib/maven/' }
}

dependencies {
    minecraft 'net.minecraftforge:forge:1.20.1-47.4.10'
    implementation fg.deobf('software.bernie.geckolib:geckolib-forge-1.20.1:4.4.9')
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
}

jar {
    manifest {
        attributes([
                'Specification-Title'     : 'OreSpawn Reforged',
                'Specification-Vendor'    : 'Santi-PdR',
                'Specification-Version'   : '3',
                'Implementation-Title'    : 'OreSpawn Reforged',
                'Implementation-Version'  : project.version,
                'Implementation-Vendor'   : 'Santi-PdR'
        ])
    }
}

// Puente temporal para assets binarios originales que se guardan en Base64 desde el conector.
apply from: 'tools/decode-staged-textures.gradle'
